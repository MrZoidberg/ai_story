/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    /* config options here */
    i18n: {
        locales: ['en', 'ru', 'ua'],
        defaultLocale: 'en',
    },
  }
  
  module.exports = nextConfig